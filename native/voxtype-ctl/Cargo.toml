[package]
name = "voxtype-ctl"
version = "0.2.0"
edition = "2021"
license = "MIT"
description = "Instant control client for VoxType"

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
strip = true
panic = "abort"
